import React from "react";
import Header from "../components/Header";

const Register = () => {
  window.scrollTo(0, 0);
  return (
    <div>
      <Header />
    </div>
  );
};

export default Register;
